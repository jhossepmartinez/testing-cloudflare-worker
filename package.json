{
  "name": "testing-cloudflare-worker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx esbuild ./src/index.ts --bundle --outdir=dist --format=esm",
    "apply": "npx dotenv -- terraform apply",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@tsndr/cloudflare-worker-jwt": "^3.2.1",
    "drizzle-orm": "^0.45.1",
    "openai": "^6.25.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260226.1",
    "dotenv": "^17.3.1",
    "dotenv-cli": "^11.0.0",
    "drizzle-kit": "^0.31.9",
    "esbuild": "0.27.3",
    "globals": "^17.3.0"
  }
}
