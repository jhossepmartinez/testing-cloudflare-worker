import { createAnalyticsLogger } from "./analytics";
import { createGitHubAuth } from "./githubAuth";
import { createQuestionRepository } from "./questionRepository";

export default {
  createAnalyticsLogger,
  createGitHubAuth,
  createQuestionRepository,
};
